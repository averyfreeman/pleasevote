---
title: Development
description: Run the PLEASE VOTE™ application locally.
---

Install dependencies with Bun:

```bash
bun install
```

Run the development server with the project’s configured script. Configure the Civic Information API key through the application’s environment settings, and do not commit secrets.

Before publishing changes, run the available build and type checks for the current React Router configuration.
